{
  "name": "rabbit-challenger",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "tsc": "tsc",
    "build": "tsc -b",
    "receive": "ts-node-dev --transpile-only --ignore-watch node_modules src/receive.ts",
    "send": "ts-node-dev --transpile-only --ignore-watch node_modules src/send.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "amqplib": "^0.8.0",
    "express": "^4.17.2"
  },
  "devDependencies": {
    "ts-node-dev": "^1.1.8",
    "@types/amqplib": "^0.8.2",
    "@types/express": "^4.17.13",
    "typescript": "^4.5.4"
  }
}
